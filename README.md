# milkbot

Discord의 `/겜플라이브` 명령어로 게임플라자 유튜브 라이브 상태를 확인하는 봇입니다.

## 실행

1. `.env.example`을 `.env`로 복사합니다.
2. `DISCORD_TOKEN`과 `YOUTUBE_API_KEY`를 입력합니다.
3. 의존성을 설치하고 봇을 실행합니다.

```shell
pip install -r requirements.txt
python bot.py
```

새 명령어를 추가하는 방법은 `bot.py`의 **새 슬래시 명령어를 추가하는 곳** 주석을 참고하세요.
